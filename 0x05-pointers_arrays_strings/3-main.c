#include "main.h"
/**
 * main - checks the code
 *
 *
 * Return: always 0
 */
int main(void)
{
	char *str;

	str = "i do not fear computers. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}

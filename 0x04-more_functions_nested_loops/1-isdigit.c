#include "main.h"
/**
 * _isdigit - returns 1 if character is a number and 0 if it is not
 * @c: parameter that returns isdigit
 *
 * Return: 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}

#include "main.h"

/**
 * _islower - prints 1 if letter is lowercase and 0 if it is not lowercase
 *@c: parameter to compare
 *
 * Return: 0
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}

#include <stdlib.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0(Success)
 */

int main(void)
{
	int Number;

	for (Number = 0; Number < 10; Number++)
		printf("%d", Number);
	printf("\n");
	return (0);
}
